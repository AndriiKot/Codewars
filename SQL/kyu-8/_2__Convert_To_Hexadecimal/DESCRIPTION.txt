to hex You have access to a table of monsters as follows:

monsters table schema:
id
name
legs
arms
characteristics

Your task is to turn the numeric columns (arms, legs) into equivalent hexadecimal values.

output table schema
legs
arms


RU:
to hex
У вас есть доступ к таблице монстров следующим образом:

monsters table schema:
id
name
legs
arms
characteristics

Ваша задача — преобразовать числовые столбцы ( arms, legs) 
в эквивалентные шестнадцатеричные значения.

схема выходной таблицы
legs
arms
