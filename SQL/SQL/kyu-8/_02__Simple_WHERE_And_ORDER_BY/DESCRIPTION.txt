For this challenge you need to create 
a simple SELECT statement that will 
return all columns from the people table 
WHERE their age is over 50

people table schema

id
name
age

You should return all people fields where their age is over 50 and order by the age descending

RU:
Для этой задачи вам нужно создать простой оператор SELECT, 
который будет возвращать все столбцы из people таблицы, ГДЕ их возраст превышает 50 лет.

people table schema

id
name
age

Вы должны вернуть все поля людей, возраст которых превышает 
50 лет, и упорядочить их по убыванию возраста.