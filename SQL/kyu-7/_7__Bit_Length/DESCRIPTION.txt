** demographics table schema **

id
name
birthday
race

you need to return the same table 
where all text fields (name & race) 
are changed to the bit length of the string.


вам нужно вернуть ту же таблицу, 
в которой все текстовые поля 
(имя и раса) изменены на длину строки в битах.