#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

changed_files=$(git diff --cached --name-only)
if echo "$changed_files" | grep -vqE "README.md|^task-scripts/"; then
  npm run pre-commit
else
  echo "\n\nPre-commit skipped, only changes in README.md or task-scripts directory.\n\n"
fi
